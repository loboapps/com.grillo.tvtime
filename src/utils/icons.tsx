import { Tv, Search, User, ChevronLeft, ChevronDown, Plus, Check } from 'lucide-react'

export const icons = {
  shows: Tv,
  search: Search,
  user: User,
  back: ChevronLeft,
  chevronDown: ChevronDown,
  plus: Plus,
  check: Check,
}
